version: 3

includes:
  shared:
    taskfile: hack/common/Taskfile_service.yaml
    flatten: true
    excludes: [] # put task names in here which are overwritten in this file
    vars:
      CODE_DIRS: '{{.ROOT_DIR}}/cmd/...'
      COMPONENTS: 'api-server task-scheduler task-worker tenant-manager tenant-manager-cli'
      REPO_URL: 'https://github.com/openkcm/cmk'

