apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cmk.fullname" . }}-test-connection"
  labels:
    {{- include "cmk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cmk.fullname" . }}:{{ "8081" }}']
  restartPolicy: Never
